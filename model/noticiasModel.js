class Noticias {
  constructor(titulo, descricao, data_publicacao, assunto, texto_materia, autor) {
      this.titulo = titulo;
      this.descricao = descricao;
      this.data_publicacao = data_publicacao;
      this.assunto = assunto;
      this.texto_materia = texto_materia;
      this.autor = autor;
  }
}

module.exports = Noticias;
